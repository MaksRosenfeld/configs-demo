management:
  endpoints:
    web:
      exposure:
        include: refresh

message-name: ${MESSAGE:third-check}

spring:
  rabbitmq:
    host: localhost
    port: 5672
    username: admin
    password: admin
